apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "platform-api.fullname" . }}-config
  labels:
    {{- include "platform-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
